(() => {
  if ((window as Record<string, unknown>).__{{ camelCase name }}Injected) return;
  (window as Record<string, unknown>).__{{ camelCase name }}Injected = true;

  console.log('[{{ displayName }}] Content script loaded');
})();
