// {{ displayName }} — Background service worker
// NOTE: Service workers have no DOM access. Use chrome.* APIs only.

chrome.runtime.onInstalled.addListener(() => {
  console.log('{{ displayName }} installed');
});
